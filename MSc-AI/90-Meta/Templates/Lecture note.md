---
type: Document
title: <% tp.file.title %>
description:
module:
about:
tags: [lecture]
created: '<% tp.date.now("YYYY-MM-DDTHH:mm:ssZ") %>'
---

# <% tp.file.title %>

<%* /* A dated record of one lecture. Episodic - it stays in the vault, it does not
       graduate. What you *learn* from it becomes a Concept note, which does.
       Set `module:` to the code (CT5145) and `about:` to the module's bundle record
       (`[[module-ct5145]]`). */ %>

**Module:**
**Date:** <% tp.date.now("YYYY-MM-DD") %>

## Notes

## Questions raised

-

## Follow up

- [ ]

## Concepts to write up

<!-- Each of these becomes a Concept note. That is where the durable value lands. -->
-
